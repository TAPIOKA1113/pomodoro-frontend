import { useState } from 'react';
import {
    FormControl,
    Input,
    useDisclosure,
    Select,
    Option
} from "@yamada-ui/react"
import { Button } from "@yamada-ui/react"

function Task() {

    return (
        <>
            <h1>タスク管理</h1>
        </>
    );
}

export default Task;